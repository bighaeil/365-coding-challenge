/**
 * ex1. 중복 제거 (Remove Duplicates)
 * 정수 배열에서 중복된 값을 제거한 새 배열을 반환
 */

function uniqueArray(arr) {
  // 여기에 풀이를 작성하세요
}

// 실행 예시
console.log(uniqueArray([1, 2, 2, 3, 3, 4])); // [1, 2, 3, 4]

module.exports = { uniqueArray };
